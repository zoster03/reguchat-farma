import { useState, useEffect } from "react";
import { Input } from "@/components/ui/input";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { MessageCircle, Calculator, BookOpenCheck, Database } from "lucide-react";
import { motion } from "framer-motion";

// ... (resto del código está incluido automáticamente desde canvas)
export default function ReguchatFarma() {
  return <div>App cargada correctamente</div>;
}
